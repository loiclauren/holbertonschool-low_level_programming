#include <stdio.h>

int mul(int a, int b) {
    return a * b;
}

int main() {
int num1 = 5; // Change these values to test different multiplications
int num2 = 3;
int result = mul(num1, num2);
printf("%d * %d = %d\n", num1, num2, result);
return 0;
}
